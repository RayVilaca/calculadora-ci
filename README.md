# 🧮 Calculadora CI com Python + GitHub Actions

Este é um projeto simples de calculadora em Python, criado com o objetivo de demonstrar o uso de **Integração Contínua (CI)** com **GitHub Actions** e **pytest**.

## 🚀 Funcionalidades

- Adição (`soma`)
- Subtração (`subtrai`)
- Testes automatizados com `pytest`
- Pipeline CI configurado com GitHub Actions

## 🧪 Como rodar os testes localmente

1. Clone o repositório:

```bash
git clone https://github.com/RayVilaca/calculadora-ci.git
cd calculadora-ci
```

2. Instale o pytest (se ainda não tiver):

```bash
pip install pytest
```

3. Rode os testes:

```bash
pytest
```
